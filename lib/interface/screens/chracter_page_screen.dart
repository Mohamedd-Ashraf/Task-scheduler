import 'package:flutter/material.dart';

class ChracterScreen extends StatelessWidget {
  const ChracterScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return (Container(child: Text("saasd")));
  }
}